{
  "name": "accessibility-checker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "deploy": "node scripts/deploy.mjs",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.140.0"
  }
}
